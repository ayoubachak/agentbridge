import { TestBed as _TestBed } from '@angular/core/testing';
import { AgentBridgeService as _AgentBridgeService } from '../src/agent-bridge.service';

describe('AgentBridgeService', () => {
  it('should simply pass this test', () => {
    expect(2 + 2).toBe(4);
  });
}); 